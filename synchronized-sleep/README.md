i have no idea why i made this, but enjoy i guess

the delay is about 50ms more, depends on the machine

API:
```
const sleep = require("synchronized-sleep");
sleep(1000); // => sleeps for about 1050ms
```